<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/last_match"
        android:enabled="true"
        android:title="@string/tab_last_match"
        android:icon="@drawable/ic_last_match"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/next_match"
        android:enabled="true"
        android:title="@string/tab_next_match"
        android:icon="@drawable/ic_next_match"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/teams"
        android:enabled="true"
        android:title="@string/teams"
        android:icon="@drawable/ic_teams"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/favorite_match"
        android:enabled="true"
        android:title="@string/favorites"
        android:icon="@drawable/ic_favorite_match"
        app:showAsAction="ifRoom"/>

</menu>
